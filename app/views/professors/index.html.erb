<h1>Listing professors</h1>

<table>
  <tr>
    <th>Emailaddress</th>
    <th>Loginpassword</th>
    <th>Currentscore</th>
    <th>Course</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @professors.each do |professor| %>
  <tr>
    <td><%= professor.emailAddress %></td>
    <td><%= professor.loginPassword %></td>
    <td><%= professor.currentScore %></td>
    <td><%= professor.course %></td>
    <td><%= link_to 'Show', professor %></td>
    <td><%= link_to 'Edit', edit_professor_path(professor) %></td>
    <td><%= link_to 'Destroy', professor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Professor', new_professor_path %>
